

import { A } from "../api.generated"

export const $$: A.arrayForEach = () => {
    return ($i, $c) => {
        $c(($) => {
            $.__forEach(($) => {
                $i($)
            })

        })
    }
}